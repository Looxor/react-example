import React from "react";
import { Overlay } from "teaset";

const FlowerParticleView = (
  <Overlay.View
    style={{alignItems: 'center', justifyContent: 'center'}}
    modal={true}
    overlayOpacity={0.01}></Overlay.View>
);

export default FlowerParticleView;
